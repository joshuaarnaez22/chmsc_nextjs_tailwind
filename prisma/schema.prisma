datasource db {
  provider     = "mysql"
  url          = "mysql://igo9sesubpljka7bisqy:pscale_pw_PBCoKr8AM7wyWYNS66wrPsxjGhCM8zEBwP9pXz1Y28d@ap-southeast.connect.psdb.cloud/clearance_db?sslaccept=strict"
  relationMode = "prisma"
}

generator client {
  provider = "prisma-client-js"
}

enum Role {
  TEACHER
  ADMIN
}

model User {
  id        String   @id @default(uuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  username  String   @db.VarChar(255)
  password  String
  email     String   @unique
  role      Role     @default(TEACHER)
  profile   Profile?
}

model Profile {
  id        String @id @default(uuid())
  user      User   @relation(fields: [profileId], references: [id], onDelete: Cascade, onUpdate: Cascade)
  profileId String @unique
  address   String
}
